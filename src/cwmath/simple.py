# example codestyle
def add_one(number:int) -> int:
    """Increase number by one.

    Examples:
        >>> add_one(5)
        6.0
        >>> add_one(6)
        7.0
        
    Args:
        number (int): a number

    Returns:
        int: number increased by one
    """
    return number + 1

