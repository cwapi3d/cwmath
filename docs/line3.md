# Line3

A frozen infinite line: origin plus a unit direction. Build with
`from_origin_direction` or `from_points`. `project` is the closest point;
`parameter` is the corresponding `t`.

::: cwmath.line3.Line3
    options:
        show_root_heading: true
        show_source: true
