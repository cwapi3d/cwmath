# Plane3

A frozen plane from a point and a (unit) normal. Zero normal raises
`ValueError`. `project` returns the foot; `contains` uses the package
tolerance.

::: cwmath.plane3.Plane3
    options:
        show_root_heading: true
        show_source: true
