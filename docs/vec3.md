# Vec3

A frozen 3D displacement. Dot, cross, magnitude, `normalized`, and `angle_to`
(radians). Normalizing the zero vector raises `ValueError`.

::: cwmath.vec3.Vec3
    options:
        show_root_heading: true
        show_source: true
